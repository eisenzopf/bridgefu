[package]
name = "bridgefu"
version = "0.9.0"
edition = "2021"
rust-version = "1.95"
description = "Programmable SIP/RTP, WebRTC/RTP, provider-control, and QUIC audio bridge"
license = "MIT"
readme = "README.md"
homepage = "https://github.com/eisenzopf/bridgefu"
repository = "https://github.com/eisenzopf/bridgefu"
keywords = ["sip", "webrtc", "rtp", "telephony", "voip"]
categories = ["network-programming", "multimedia::audio"]
publish = false

[workspace]
members = ["tools/recipe-qualification"]
default-members = ["."]
resolver = "2"

[dependencies]
# Bridgefu consumes the coordinated rvoip release from crates.io. Exact
# component versions keep the signaling/media type graph aligned and make
# Cargo.lock the complete reproducible dependency input.
rvoip-amazon-connect = { version = "=0.3.5", features = ["server", "aws-control"] }
rvoip-auth-core = "=0.3.5"
rvoip-core = "=0.3.5"
rvoip-moq = { version = "=0.3.5", features = ["relay-runtime"] }
rvoip-redis = { version = "=0.3.5", features = ["moq"] }
rvoip-quic = "=0.3.5"
rvoip-uctp = "=0.3.5"
rvoip-sip = { version = "=0.3.5", features = ["opus"] }
rvoip-webrtc = { version = "=0.3.5", features = ["signaling-whip", "signaling-ws", "tls-rustls"] }
telnyx = { version = "=0.1.0", default-features = false, features = ["calls", "webhooks", "rustls-ring"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
quinn = { version = "0.11", default-features = false, features = ["runtime-tokio", "rustls-ring"] }
rustls-pemfile = "2"

tokio = { version = "1", features = ["full"] }
tokio-util = { version = "0.7", features = ["rt"] }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_ignored = "0.1.14"
serde_yaml = "0.9"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
tracing-opentelemetry = { version = "0.33", default-features = false }
opentelemetry = { version = "0.32", default-features = false, features = ["trace"] }
opentelemetry_sdk = { version = "0.32", default-features = false, features = ["trace"] }
opentelemetry-otlp = { version = "0.32", default-features = false, features = ["trace", "grpc-tonic", "tls-ring", "tls-webpki-roots"] }
axum = "0.7"
axum-server = { version = "0.7.3", default-features = false, features = ["tls-rustls-no-provider"] }
tower-http = { version = "0.6", features = ["trace", "request-id"] }
serde_json = "1"
sqlx = { version = "0.8.6", default-features = false, features = ["runtime-tokio-rustls", "sqlite", "postgres", "chrono", "uuid", "migrate", "macros"] }
async-trait = "0.1"
base64 = "0.22"
bytes = { version = "1", features = ["serde"] }
chrono = { version = "0.4", features = ["serde"] }
dashmap = "6"
futures-util = "0.3"
hmac = "0.12"
ipnet = "2.12"
jsonwebtoken = "9.3"
ring = "0.17"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
redis = { version = "1.0.4", default-features = false, features = ["tokio-rustls-comp", "connection-manager", "streams", "script"] }
sha1 = "0.10"
sha2 = "0.10"
thiserror = "1"
subtle = "2"
url = "2"
uuid = { version = "1", features = ["v4", "serde"] }
zeroize = "1"
metrics = "0.23"
metrics-exporter-prometheus = { version = "0.15", default-features = false }

[dev-dependencies]
rvoip-media-core = { version = "=0.3.5", features = ["opus"] }
rcgen = "0.14"
tower = { version = "0.5", features = ["util"] }
webrtc = { package = "rvoip-webrtc-stack", version = "=0.3.5" }

[patch.crates-io]
# Pin the coordinated 0.3.5 graph to the reviewed backport commit. It includes
# the wildcard-Contact dialog routing fix tracked in issue 167, the Amazon
# Connect late-remote-track fix, bounded driver backpressure, and primary audio
# compatibility when an endpoint does not negotiate SDES MID. It also prevents
# a healthy media sink from being evicted while a new WebRTC/Connect route is
# still establishing and exposes sinkless-frame evidence. It also provides a
# bounded inclusive WebRTC UDP allocator and preserves the browser's offered
# codec preference order when generating an SDP answer. Patching every rvoip
# crate Bridgefu consumes keeps shared signaling/media types sourced from one
# immutable revision.
rvoip-amazon-connect = { git = "https://github.com/eisenzopf/rvoip.git", rev = "7eb6f3f040139d7bad24629968284a683bf77f68" }
rvoip-auth-core = { git = "https://github.com/eisenzopf/rvoip.git", rev = "7eb6f3f040139d7bad24629968284a683bf77f68" }
rvoip-core = { git = "https://github.com/eisenzopf/rvoip.git", rev = "7eb6f3f040139d7bad24629968284a683bf77f68" }
rvoip-media-core = { git = "https://github.com/eisenzopf/rvoip.git", rev = "7eb6f3f040139d7bad24629968284a683bf77f68" }
rvoip-moq = { git = "https://github.com/eisenzopf/rvoip.git", rev = "7eb6f3f040139d7bad24629968284a683bf77f68" }
rvoip-quic = { git = "https://github.com/eisenzopf/rvoip.git", rev = "7eb6f3f040139d7bad24629968284a683bf77f68" }
rvoip-redis = { git = "https://github.com/eisenzopf/rvoip.git", rev = "7eb6f3f040139d7bad24629968284a683bf77f68" }
rvoip-sip = { git = "https://github.com/eisenzopf/rvoip.git", rev = "7eb6f3f040139d7bad24629968284a683bf77f68" }
rvoip-uctp = { git = "https://github.com/eisenzopf/rvoip.git", rev = "7eb6f3f040139d7bad24629968284a683bf77f68" }
rvoip-webrtc = { git = "https://github.com/eisenzopf/rvoip.git", rev = "7eb6f3f040139d7bad24629968284a683bf77f68" }
rvoip-webrtc-stack = { git = "https://github.com/eisenzopf/rvoip.git", rev = "7eb6f3f040139d7bad24629968284a683bf77f68" }

# bridgefu is the build root, so this profile governs the WHOLE dependency tree
# (it overrides the rvoip workspace's `lto = true, codegen-units = 1`, which are
# memory- and time-hungry across the full SIP/WebRTC/AWS tree). `thin` LTO + many
# codegen units keeps the on-instance build fast and within RAM, while opt-level 2
# is plenty for a POC gateway whose hot path is a single audio bridge.
[profile.release]
lto = "thin"
codegen-units = 16
opt-level = 2
