<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- bridgefu multi-tenant routing test: UNKNOWN tenant → 404 Not Found.

     Sends an INVITE whose user part matches no tenant (and with no
     default_tenant configured) and expects the CONTRACTS B.4 rejection:
     404 Not Found + bridgefu_unknown_tenant_total incremented on /metrics.

     Run:  sipp -sf bridgefu_unknown_tenant.xml <host>:5060 -s no-such-tenant -m 1 -->

<scenario name="bridgefu - unknown tenant is rejected 404">

  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: "Vapi caller" <sip:vapi@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:vapi@[local_ip]:[local_port];transport=[transport]>
      Subject: bridgefu unknown-tenant test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=vapi [call_number] [call_number] IN IP[local_ip_type] [local_ip]
      s=bridgefu-sipp-unknown-tenant-test
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 8 101
      a=rtpmap:8 PCMA/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-15
      a=sendrecv

    ]]>
  </send>

  <!-- The SIP stack may auto-send provisionals before the routing decision. -->
  <recv response="100" optional="true" />
  <recv response="180" optional="true" />
  <recv response="183" optional="true" />

  <!-- The B.4 contract: no matching tenant, no default → 404. -->
  <recv response="404" rtd="true" />

  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: "Vapi caller" <sip:vapi@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

  <ResponseTimeRepartition value="100, 250, 500, 1000, 2000" />

</scenario>
